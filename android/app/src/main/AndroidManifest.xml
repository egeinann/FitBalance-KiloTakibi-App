<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Bildirimler için gerekli izinler -->
    <uses-permission android:name="android.permission.VIBRATE" /> // titreşim izni
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" /> // başlangıç izni
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" /> // Pil optimizasyonunu göz ardı etme
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/> // Ön plan servisi izni
    <uses-permission android:name="android.permission.WAKE_LOCK"/> // Ekranı uyandırma
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/> // Android 13 ve sonrası için bildirim gönderme izni zorunlu, ekle
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" /> // Belirli bir saatte bildirim gösterme izni
    <application
        android:label="kilo_takibi_uyg"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- Bildirim alacak receiver bileşeni -->
        <receiver android:name=".NotificationReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Don't delete the meta-data below. -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- Required to query activities that can process text -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
